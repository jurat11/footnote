# Runtime dependencies for the Streamlit Community Cloud deployment (app.py).
# The `footnote` package itself is imported directly from the repo, so it is not listed.
streamlit>=1.36
pandas>=2.0
httpx>=0.27
pydantic>=2.6
typer>=0.12
